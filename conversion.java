/* BinToHex :- to get binary number "Integer.parseInt(s.nextLine(),2):"     to convert to hex use "Integer.toHexString()"
   BintoDec :- to convert to dec "decimal=Integer.parseInt("1011",2); "  
   DectoBin:-  to convert to bin "Integer.toBinaryString(decimal); " 
   HextoDec to convert hex to dec " d=Integer.parseInt("15a",16);"          to get hex "Integer.parseInt(s.nextLine(),16);

/* 
